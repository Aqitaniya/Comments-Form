
 * 1) Создана форму коментариев
 * 2) Визуализация при помощи Bootstrap
 * 3) все коментарии записать в базу данных
 * 4) при нажатии кнопки "отправить" комментарии добавляется в базу с помощью Ajax
 * 5) для работы с формой созданы php-классы (mvc)
 * 6) под формой отображается список коментариев
 * 7) реализована отправка информации на email
 * 8) реализована валидация полей формы
 * 9) реализована загрузка прикреплённого файла (если такой имеется) в папку /uploads
 *
 * Поля формы:
 * 1) email
 * 2) ФИО
 * 3) телефон
 * 4) комментарий
 * 5) прикрепить файл
 *
 * Формат вывода коментариев:
 * #ID_коментария #Дата
 * #ФИО (#email)
 * #Комментарий
